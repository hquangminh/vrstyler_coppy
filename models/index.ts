export type ContainerSize = 'default' | 'large' | 'free';

export type CountAggregate = {
  aggregate: {
    count: number | null;
  };
};
export type LanguageSupportType = 'en' | 'kr' | 'jp' | 'vn';
